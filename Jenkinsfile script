node {
    stage('Checkout Code') {
        checkout scm
    }

    stage('Build') {
        echo "Build stage running"
    }

    stage('Test') {
        echo "Test stage running"
    }

    stage('Deploy') {
        echo "Deploy stage running"
    }
}
